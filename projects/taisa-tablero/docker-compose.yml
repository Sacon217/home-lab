services:
  taisa-tablero:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: taisa-tablero
    restart: unless-stopped
    environment:
      GITHUB_TOKEN: ${GITHUB_TOKEN}
    networks:
      - edge

networks:
  edge:
    external: true
    name: core-services_edge
